-- After creating your first user via /signup, promote them to admin:
-- replace the email below, then run in Supabase SQL editor.
--
-- update public.profiles
-- set role = 'admin'
-- where email = 'you@example.com';

select 'Promote an admin by updating profiles.role after signup.' as instruction;
